#import "functions.typ": all_pcb_data

#let name = "PM_DBG-FFC"

#pagebreak()

== #name <PM-DBG_FFC>

Плата для упрощения отладки. Вставляется в разрыв кабеля FFC между платами. На плате только разъем для подключения осциллографа. Также есть две перемычки (+5V и +3.3V) для возможности измерения потребляемой мощности.

#all_pcb_data(name: name)
